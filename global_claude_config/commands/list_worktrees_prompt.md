---
model: haiku
description: List all git worktrees with their branch and status
allowed-tools: Bash
---

# List Git Worktrees

Run `git worktree list` and display the results. Note which worktrees are under `.worktrees/`.
